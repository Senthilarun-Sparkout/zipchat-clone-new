package com.chat.zipchat.clone.Model.Contact;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.chat.zipchat.clone.Model.DaoSession;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONTACT_RESPONSE".
*/
public class ContactResponseDao extends AbstractDao<ContactResponse, Long> {

    public static final String TABLENAME = "CONTACT_RESPONSE";

    /**
     * Properties of entity ContactResponse.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Contact_id = new Property(0, Long.class, "contact_id", true, "_id");
        public final static Property Status = new Property(1, boolean.class, "status", false, "STATUS");
    }

    private DaoSession daoSession;


    public ContactResponseDao(DaoConfig config) {
        super(config);
    }
    
    public ContactResponseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CONTACT_RESPONSE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: contact_id
                "\"STATUS\" INTEGER NOT NULL );"); // 1: status
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CONTACT_RESPONSE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ContactResponse entity) {
        stmt.clearBindings();
 
        Long contact_id = entity.getContact_id();
        if (contact_id != null) {
            stmt.bindLong(1, contact_id);
        }
        stmt.bindLong(2, entity.getStatus() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ContactResponse entity) {
        stmt.clearBindings();
 
        Long contact_id = entity.getContact_id();
        if (contact_id != null) {
            stmt.bindLong(1, contact_id);
        }
        stmt.bindLong(2, entity.getStatus() ? 1L: 0L);
    }

    @Override
    protected final void attachEntity(ContactResponse entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ContactResponse readEntity(Cursor cursor, int offset) {
        ContactResponse entity = new ContactResponse( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // contact_id
            cursor.getShort(offset + 1) != 0 // status
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ContactResponse entity, int offset) {
        entity.setContact_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setStatus(cursor.getShort(offset + 1) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ContactResponse entity, long rowId) {
        entity.setContact_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ContactResponse entity) {
        if(entity != null) {
            return entity.getContact_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ContactResponse entity) {
        return entity.getContact_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
